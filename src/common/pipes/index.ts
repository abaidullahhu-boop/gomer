/**
 * Custom pipes live here. Validation is handled globally by Nest's built-in
 * ValidationPipe (configured in main.ts), so this barrel is intentionally empty
 * until project-specific pipes are introduced.
 */
export {};
